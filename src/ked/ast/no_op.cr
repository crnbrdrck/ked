module Ked
  module AST
    # Node representing an empty statement
    # NOTE: This might be removed at a later point if no use is found for it, it is here as it was pointed out in the tutorial and there might be a use for it yet
    class NoOp < Node
    end
  end
end
